"use client";
import { useState } from "react";
import { ShareActions } from "@/components/share/ShareActions";
import { resultSharePayload } from "@/components/share/templates";
import { ResultDocument } from "./ResultDocument";
import { SERVICE_FIELDS } from "@/lib/ai/stalls";
import type { StallResult } from "@/lib/market/types";
export function ServiceDesk({ slug, name }: { slug: string; name: string }) { const field = SERVICE_FIELDS[slug] ?? { label: "办理内容", placeholder: "写下你要办的事" }; const [input, setInput] = useState(""); const [result, setResult] = useState<StallResult | null>(null); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); async function submit() { setBusy(true); setError(""); try { const res = await fetch(`/api/stalls/${slug}/generate`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ input }) }); const body = await res.json(); if (!res.ok) throw new Error(body.error); setResult(body.result); void fetch("/api/metrics", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ slug, event: "generation" }) }); } catch { setError("摊主暂时去沏茶了，原文还在，稍后再试。"); } finally { setBusy(false); } } return <section className="service-desk"><p className="desk-seal">{name} · 业务受理单</p><label>{field.label}<textarea value={input} onChange={(event) => setInput(event.target.value)} placeholder={field.placeholder} maxLength={500} /></label><button disabled={!input.trim() || busy} onClick={submit}>{busy ? "正在替你走流程" : "递交材料"}</button>{error && <p className="desk-error">{error}</p>}{result && <><ResultDocument result={result} /><ShareActions template={result.shareTemplate} payload={resultSharePayload(result.title, result.summary)} filename={`${slug}-9527.png`} onSaved={() => void fetch("/api/metrics", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ slug, event: "image_save" }) })} /></>}</section>; }
